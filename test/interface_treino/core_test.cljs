(ns interface-treino.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [interface-treino.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
